langgraph>=0.2.0
langchain-openai>=0.1.0
pandas>=2.0.0
numpy>=1.24.0
akshare>=1.14.0
tushare>=1.4.0
streamlit>=1.35.0
chromadb>=0.5.0
sentence-transformers>=2.7.0
jieba>=0.42.1
openpyxl>=3.1.0
plotly>=5.18.0
matplotlib>=3.8.0
seaborn>=0.13.0
scipy>=1.12.0
alphalens-reloaded>=0.4.0
statsmodels>=0.14.0
pyyaml>=6.0

# ── 因子精炼厂（六阶段冶炼流水线）可选依赖 ──
# 基础（演示/CI 必备，无重依赖）：
scikit-learn>=1.3.0        # LASSO 浮选筛选 / LassoCV
joblib>=1.3.0             # 并行加速支撑
# 深度学习（Transformer 向量化表征，缺省自动降级 numpy 实现）：
torch>=2.1.0
# 强化学习（MaskablePPO 因子搜索，缺省自动降级集束搜索启发式）：
stable-baselines3>=2.2.0
sb3-contrib>=2.2.0
gymnasium>=0.29.0

# ── 工程化与可复现性（方向二，可选依赖，按需安装） ──
mlflow>=2.10.0          # 实验追踪（backend=mlflow）；不装则自动降级本地 JSONL
optuna>=3.5.0           # 超参搜索（src/engine/hpo.py）；不装则提示安装
# ── 因子生成升级（方向三，蒸馏专用，可选重依赖，按需安装） ──
# peft>=0.10.0         # LoRA 微调（scripts/distill_factors.py 蒸馏用）
# transformers>=4.40.0 # 微调基座（蒸馏用，体积大）
# datasets>=2.18.0     # 训练数据集加载（蒸馏用）
